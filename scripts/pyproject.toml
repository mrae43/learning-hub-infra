[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "scripts"
version = "0.1.0"
requires-python = ">=3.12"
license = "MIT"

[tool.setuptools]
package-dir = {"scripts" = "."}
packages = ["scripts"]
