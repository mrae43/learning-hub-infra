[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "depth-dive"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["core"]

[tool.uv.sources]
core = { workspace = true }

[tool.hatch.build.targets.wheel]
packages = ["src/depth_dive"]
